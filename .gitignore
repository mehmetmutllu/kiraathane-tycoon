# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Test / build artifacts
test-results
playwright-report
coverage
.playwright-mcp/
# kök dizindeki tanı/önizleme ekran görüntüleri (oyun assetleri public/ altında)
/*.png
# Kök dizine kopyalanan APK (cihaza atmak için; build çıktısı, git'e girmez)
/*.apk

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# git worktree klasörleri (EnterWorktree) — repoya girmez
.claude/worktrees/

# Rapor artifact kopyalari (tools/embed-rapor.mjs uretir; kareler data URI olarak gomulu)
*.artifact.html

# Karar panolarinin URETILMIS ses yuku (data URI). Ayni gerekce: araclar yeniden uretir
# (tools/ses-karar-paketi.ts · tools/muzik-karar-paketi.ts), depoda megabaytlarca base64
# tutmanin karsiligi yok — panonun kendisi (index.html) commit'li ve yayinlanmis durumda.
# NOT: yeniden uretmek `indirilen/` ve `indirilen-muzik/` altindaki paketleri ister.
docs/pano/ses-karar/sesler.js
docs/pano/ses-karar/muzik.js

# S14 ölçüm turunda indirilen karakter ADAYLARI (repoya girmez; karar D-1xx ile seçilen
# dosyalar manifestle birlikte girer)
public/assets/models/_aday-karakter/

# S17 ses turunda indirilen Kenney PAKETLERI (repoya girmez; karar D-1xx ile secilen
# dosyalar kunyesiyle public/assets/audio/ altina girer)
indirilen/

# S19a yemek turunda indirilen Kenney Food Kit ADAYLARI (repoya girmez; kullanici modeli
# secerse manifestle birlikte `public/assets/models/`e tasinir — stil kilidi karari onun).
# git bash çökme dökümü. Satır-içi yorum YAZILMAZ: .gitignore yalnız satır BAŞINDAKİ # ile
# yorum sayar, sonrasında yazılan her şey kalıbın parçası olur.
# S24'te bu dosya kökte belirdi ve Vite'ın dosya izleyicisi ona EBUSY ile takılıp çöktü.
*.stackdump

# S9 ses turunda indirilen MUZIK adaylari (repoya girmez; secilen parca kunyesiyle
# public/assets/audio/ altina girer)
indirilen-muzik/
