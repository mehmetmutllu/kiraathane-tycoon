import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.kosekiraathanesi.game',
  appName: 'Köşe Kıraathanesi',
  webDir: 'dist'
};

export default config;
